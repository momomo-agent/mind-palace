# Harness Engineering：在 Agent-First 世界中用 Codex 构建产品

> **作者**: OpenAI 团队  
> **日期**: 2026-02-14  
> **来源**: [openai.com](https://openai.com/index/harness-engineering/)  
> **标签**: AI, Agent, Codex, 软件工程, 零手写代码

💡 **看点：OpenAI 内部团队做了一个极端实验——从空仓库开始，用 Codex 生成全部代码，0 行手写。5 个月、3 人团队、1500 个 PR、百万行代码。核心发现：工程师的角色从写代码变成了设计环境、定义意图、构建反馈循环。**

---

## 实验设定

团队从一个空 Git 仓库开始，刻意约束自己不写任何代码。所有代码——应用逻辑、测试、CI 配置、文档、可观测性、内部工具——全部由 Codex 生成。

**核心数据：**
- **百万行代码**，涵盖应用、基础设施、工具、文档
- **~1500 个 PR**，3 人团队起步（后扩展到 7 人）
- **人均每天 3.5 个 PR**，且随团队扩大吞吐量还在增长
- 估算效率是手写代码的 **10 倍**

> "人类掌舵，Agent 执行。"

---

## 工程师角色的重新定义

**早期进展比预期慢**——不是因为 Codex 不行，而是环境描述不够充分。Agent 缺少完成高层目标所需的工具、抽象和内部结构。

工程师的核心工作变成了：
- **深度优先拆解**：把大目标拆成小构建块，让 Agent 逐步构建
- **失败时不是"再试一次"**，而是问"缺了什么能力？如何让它对 Agent 可读且可执行？"
- **几乎完全通过 prompt 交互**：描述任务 → 运行 Agent → Agent 开 PR
- **Agent 自审自改**：Codex 自己 review 代码，请求其他 Agent review，循环迭代直到满意

---

## 提升应用可读性

随着代码吞吐量增加，瓶颈变成了人类 QA 能力。解决方案：**让应用本身对 Agent 可读**。

- **每个 git worktree 可独立启动应用**，Codex 能为每个变更启动独立实例
- **接入 Chrome DevTools Protocol**，Agent 能直接操作 DOM 快照、截图、导航
- **本地可观测性栈**：日志、指标、链路追踪暴露给 Agent，用 LogQL/PromQL 查询
- **单次 Codex 运行可持续 6 小时以上**（通常在人类睡觉时）

---

## 仓库知识作为唯一真相源

**核心教训：给 Agent 一张地图，而不是一本千页手册。**

为什么不能用巨大的指令文件：
- **上下文是稀缺资源**，巨大指令文件会挤掉任务和代码
- **什么都重要 = 什么都不重要**，Agent 会局部模式匹配而非有意导航
- **即时腐烂**，单体手册变成过时规则的坟场
- **难以验证**，无法做机械化检查

**解决方案：AGENTS.md 是目录，不是百科全书。**
- AGENTS.md 约 100 行，作为地图指向更深的文档
- 结构化的 `docs/` 目录是真正的知识库
- 设计文档、架构文档、质量评分、执行计划都版本化存储在仓库中
- **渐进式披露**：Agent 从小而稳定的入口开始，被教会去哪里找更多信息

**机械化执行：**
- 专用 linter 和 CI 验证知识库的时效性和交叉链接
- "文档园丁" Agent 定期扫描过时文档并开 PR 修复

---

## Agent 可读性是目标

> "从 Agent 的视角看，运行时无法访问的东西等于不存在。"

- Slack 讨论、Google Docs、人脑中的知识——Agent 看不到就等于没有
- 团队持续把更多上下文推入仓库
- **偏好"无聊"技术**：可组合、API 稳定、训练集中有大量表示
- 有时让 Agent 重新实现功能比依赖不透明的第三方库更划算

---

## 架构与品味的执行

**执行不变量，而非微管理实现。**

- 严格的分层架构模型，每个业务域分为固定层级
- 依赖方向严格验证
- CI 中运行架构 linter，违规直接阻断合并
- **品味也可以编码**：通过 lint 规则、示例代码、风格指南让 Agent 理解审美标准

---

## 关键洞察

1. **环境 > 指令**：与其写更好的 prompt，不如构建更好的环境
2. **地图 > 手册**：100 行 AGENTS.md + 结构化文档 > 巨大的指令文件
3. **可读性是第一优先级**：代码、日志、UI 都要对 Agent 可读
4. **机械化执行**：用 linter 和 CI 而非人工 review 来保证质量
5. **Agent 自审循环**：减少人类 review 负担，Agent 之间互相审查
6. **"无聊"技术更好**：可预测、可组合、训练集覆盖好

---

📎 **相关阅读**

- [Effective harnesses for long-running agents — Anthropic](https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents) — Anthropic 的长时间 Agent 工程实践：初始化 Agent + 编码 Agent 增量推进
- [擴展長時間自主程式開發的規模 — Cursor](https://cursor.com/zh-Hant/blog/scaling-agents) — Cursor 的多 Agent 并行开发实践：规划者+执行者架构
- [Anthropic Barry Zhang — 如何构建有效 Agent](https://x.com/indigox/status/1909468804930842696) — Agent 系统演进、场景选择、保持简单、像 Agent 一样思考
