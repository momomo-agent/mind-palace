# 让 AI 记住你说过的每一句话：零成本记忆系统完整构建记录

> **作者**: Ray Wang (@wangray)  
> **日期**: 2026-02-15  
> **来源**: [x.com](https://x.com/wangray/status/2022833678595035519)  
> **标签**: AI, Agent, 记忆系统, OpenClaw

💡 **看点：基于 OpenClaw 多 Agent 团队 17 天实战，从"聊完就忘"迭代到结构化持久记忆。核心发现：纯 Markdown 文件的记忆准确率（74%）高于专业向量数据库方案（68.5%）。**

---

## 核心问题

AI Agent 的致命缺陷：没有真正的记忆。上下文窗口看起来像记忆，但会被压缩、丢失、重启后清零。

> "你不写进文件的东西 = 你从来不知道的东西。"

---

## 架构：三层记忆 + 三层检索

### 三层记忆

- **工作台 NOW.md** — 当前状态看板，每次 Heartbeat 刷新，只放事实状态
- **日记 memory/YYYY-MM-DD.md** — 当日事件流水，实时追加，最完整的信息源
- **长期智慧 MEMORY.md** — 跨天教训精华，极少更新，只放跨场景通用智慧

**关键原则：不混用。** NOW 放状态，日记放细节，MEMORY 放智慧。

### 三层检索

1. **L1: INDEX.md** — 目录级扫描（1秒），大多数查询在这里命中
2. **L2: 目录 + grep** — 文件级搜索（3秒）
3. **L3: 语义搜索** — 全文向量检索（5秒）

---

## V1 朴素方案的问题（Day 1-10）

1. **MEMORY.md 越来越长** — 43 条教训挤在一个文件里
2. **日记是流水账** — 找某个决策得翻好几天
3. **没有分类** — 投资教训和 cron 调度教训混在一起
4. **写入不可靠** — Edit 工具精确匹配失败率 15-20%
5. **重复劳动** — checkpoint cron 和 heartbeat 做着重叠的事

---

## V2 结构化记忆（Day 11-15）

### 灵感

研究 ClawVault 项目后发现反直觉结论：**纯 Markdown 文件的记忆准确率（74%）高于专业向量数据库方案（68.5%）。** 原因：结构化 Markdown 对 LLM 天然友好。

### 改造：加目录，加类型

```
memory/
  INDEX.md              ← 总索引
  YYYY-MM-DD.md         ← 每日日记
  decisions/            ← 决策记录（带 YAML frontmatter）
  lessons/              ← 经验教训（按主题归档）
  people/               ← 人物档案
  projects/             ← 项目档案
  preferences/          ← 偏好设定
  .obsidian/            ← Obsidian 兼容配置
```

**经验教训按主题组织，不按时间。** 同一主题的所有教训追加到同一个文件。

### 内容回填

用子 Agent（Sonnet）自动扫描 17 天日记，提取出：
- **17 个决策**（从 Agent 团队结构到域名选择）
- **10 个经验主题**（从 cron 调度到浏览器自动化）
- **6 个人物档案 + 6 个项目档案**
- 总计 **54 个 Markdown 文件，838 行结构化内容**

---

## 写入机制：从 Cron 到 Heartbeat Skill

### 踩过的坑

- **Edit 精确匹配** — 文件内容和预期不一致就失败，失败率 15-20%
- **Write 覆盖** — Gemini Flash 倾向用 Write 重写整个文件，直接覆盖 12 小时的内容
- **Checkpoint Cron 看不到对话** — isolated session 完全看不到主 session 对话
- **重复劳动** — Heartbeat 和 checkpoint 做着重叠的事但都没做好

### 解决方案：统一到 Heartbeat Skill

把记忆写入封装成 Skill，在每次 heartbeat 时由主 session 执行。

**写入方式：`printf >>` 大法**
- `>>` 是追加，永远不会覆盖
- `printf` 比 `echo` 更可靠
- 不依赖任何工具的精确匹配逻辑
- 文件不存在时自动创建

合并后每天省 $2-3，写入质量更高。

---

## 备份：三重保障

1. **本地文件**（实时）
2. **Obsidian Sync**（实时同步到手机）
3. **GitHub**（每晚 23:00 自动 push，版本历史）

整个 memory/ 目录就是一个 Obsidian vault，Graph View 按目录着色。

---

## 总结

1. **文件就是记忆** — Markdown 比向量数据库更可靠
2. **三层分离** — NOW/日记/MEMORY 不混用
3. **按主题组织** — 不按时间，同主题追加到同一文件
4. **`printf >>` 永远不会覆盖** — Edit 和 Write 都可能
5. **合并重复系统** — 更便宜、更准确
6. **INDEX.md 是关键** — Agent 启动时只需读一个文件
7. **Obsidian 兼容** — 让人类也能直观浏览 Agent 记忆

---

📎 **相关阅读**

- [Harness Engineering — OpenAI](https://openai.com/index/harness-engineering/) — OpenAI 用 Codex 零手写代码构建产品：AGENTS.md 当目录不当百科，仓库知识是唯一真相源
- [Effective harnesses for long-running agents — Anthropic](https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents) — 长时间 Agent 工程实践：progress 文件 + JSON feature list + 增量推进
